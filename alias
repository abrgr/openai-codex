# openai-codex
source "$(dirname "${BASH_SOURCE[0]}")/docker-cli-run.sh"

function openai-codex() {
  docker_cli_run \
    --image openai-codex \
    --cmd codex \
    --env "OPENAI_API_KEY=$(cat ~/.openai)" \
    -- "$@"
}
